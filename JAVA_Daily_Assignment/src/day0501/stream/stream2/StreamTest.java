package day0501.stream.stream2;

public class StreamTest {
    // 스트림 가공하기

    /*
    많은 양의 데이터를 분석 및 가공, 축소하는 것(Reduction)
    데이터의 합계, 평균, 개수, 최대최소값은 Reduction의 결과
    중간 처리 작업인 필터링(filtering), 매핑(mapping), 정렬(sorting), 그룹핑(grouping)이 필요
     */
}
